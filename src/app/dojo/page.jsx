import DojoPage from "./DojoPage";

export const metadata = {
  title: "BuildNinja Dojo | Interactive Sandbox Environment",
  description: "Experiment with BuildNinja CI/CD in a safe, interactive sandbox. No setup required, just your browser.",
};

export default function Page() {
  return <DojoPage />;
}
